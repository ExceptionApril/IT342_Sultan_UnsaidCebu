# Database – Supabase PostgreSQL via Session Pooler (IPv4)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres.ddybaipugbtjeyyvdkgz}
spring.datasource.password=${DB_PASSWORD:ASA2NVui28750ZGH}

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT  (override JWT_SECRET in production)
jwt.secret=${JWT_SECRET:unsaidcebu-jwt-secret-key-please-change-in-production-min256bits}
jwt.expiration.ms=86400000

# Server
server.port=8080
